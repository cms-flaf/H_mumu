
[meta]
results_dir = "/afs/cern.ch/user/a/ayeagle/H_mumu/Studies/DNN/results"
use_cuda = true

[dataloader]
additional_cut = 'VBF_JetVeto'
valid_size = 0.15
test_size = 0.40
signal_types = [31]
columns_config = '/afs/cern.ch/user/a/ayeagle/H_mumu/Studies/DNN/ds_setup/general.yaml'

[network]
layer_list = [60, 60, 60, 30, 1]

[training]
batch_size = 500
epochs = 3000
early_stop = false
patience = 10

[optimizer]
lr = 1e-05
momentum = 0.7
dampening = 0.2
weight_decay = 0.0
